import { atom } from "recoil";

export const currentComponentIndexState = atom({
  key: "currentComponentIndex",
  default: 0,
});
export const resulstDataState = atom({
  key: "reulstData",
  default: [],
});
